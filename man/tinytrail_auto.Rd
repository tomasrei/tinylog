% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_trail.R
\name{tinytrail_auto}
\alias{tinytrail_auto}
\title{Register a script and automatically track all write/save calls}
\usage{
tinytrail_auto(
  description,
  data_source = NULL,
  pin_to_top = FALSE,
  record_runtime = TRUE,
  name = NULL,
  extra_hooks = NULL
)
}
\arguments{
\item{description}{Character. Description of what the script does.}

\item{data_source}{Character. Optional. Enter the sources of data used in
this script — name the dataset or survey, not a file path
(e.g. \code{"Current Population Survey (BLS)"}).}

\item{pin_to_top}{Logical. Pin this script to the top of the trail. Useful
for a \code{main.R} that sources other scripts — keeps it visible at the top of
\verb{_tinytrail.yaml} regardless of alphabetical order. Default \code{FALSE}.}

\item{record_runtime}{Logical. Record elapsed time on exit. Default \code{TRUE}.}

\item{name}{Character. Override the auto-detected script name. Useful in
testing or when auto-detection is not available.}

\item{extra_hooks}{Named character vector of additional write functions to
intercept. Names are function identifiers (\code{"fn"} for a function defined
in the script, or \code{"pkg::fn"} for a package function), values are the
name of the file-path argument in that function. Example:
\code{c(my_save = "path", "sf::st_write" = "dsn")}. Functions from packages
that are not installed are silently skipped.}
}
\value{
\code{name} (the script name), invisibly. Called for its side effect of
creating or updating the YAML trail file in the project root.
}
\description{
Drop-in alternative to \code{tinytrail()}. Call once near the top of every
script. In addition to registering the script in \verb{_tinytrail.yaml}, it
hooks common write functions (\code{write.csv}, \code{saveRDS}, \code{readr::write_csv},
\code{ggplot2::ggsave}, etc.) so their output file paths are recorded
automatically — no \code{tinytrail_write()} wrapper needed on each save call.
Hooks are silently removed when the script exits.
}
\examples{
\donttest{
withr::with_tempdir({
  writeLines("Version: 1.0", "DESCRIPTION")
  withr::with_options(
    list(.tinytrail_registry_path = NULL, .tinytrail_current_script = NULL,
         .tinytrail_traced_fns = NULL, .tinytrail_hooks_table = NULL), {

    tinytrail_auto(
      description    = "Clean and reshape survey data",
      data_source    = "Current Population Survey (BLS)",
      record_runtime = FALSE,
      name           = "clean.R"
    )

    write.csv(mtcars, "cars.csv")   # recorded automatically
    saveRDS(mtcars,   "cars.rds")   # recorded automatically
  })
})
}
}
