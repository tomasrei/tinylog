% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinylog.R
\name{tinylog_write}
\alias{tinylog_write}
\alias{tl_write}
\alias{tinylog_output}
\alias{tl_output}
\title{Record an output file path in the registry}
\usage{
tinylog_write(file)

tl_write(file)

tinylog_output(...)

tl_output(...)
}
\arguments{
\item{file}{Character. Absolute path to the output file.}

\item{...}{Arguments passed to \code{\link[=tinylog_write]{tinylog_write()}} (deprecated aliases only).}
}
\value{
\code{file}, invisibly.
}
\description{
Wraps the file path argument of any save call. Registers the path under the
current script's registry entry and returns the path unchanged, so it can be
dropped inline into any save function.
}
\details{
\code{tinylog_output()} and \code{tl_output()} are deprecated aliases kept for
backward compatibility; use \code{tl_write()} for the short form.

Requires \code{\link[=tinylog]{tinylog()}} to have been called first in the same session.
}
\examples{
\donttest{
tmp <- tempfile()
dir.create(tmp)
writeLines("Version: 1.0", file.path(tmp, "DESCRIPTION"))
old_wd <- setwd(tmp)
tinylog("raw/data.csv", "example script", name = "script.R", record_runtime = FALSE)
out <- tinylog_write(file.path(tmp, "output.csv"))
setwd(old_wd)
unlink(tmp, recursive = TRUE)
}
}
